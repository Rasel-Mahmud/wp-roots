version: 0.2

phases:
  install:
    runtime-versions:
        php: 7.3
        nodejs: 12
  pre_build:
    commands:
      - echo Running Pre-Build script...
      - echo pre build started on `date`
      - ./script/buildscipt.sh
      - echo `pwd`
  build:
    commands:
      - echo Running Build...
  post_build:
    commands:
      - echo Running post_build...
      - echo Build completed on `date`
artifacts:
    files: "**/*"